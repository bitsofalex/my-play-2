# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /assets/javascripts/routes  controllers.Application.javascriptRoutes

POST	/bodyastext					controllers.Application.bodyAsText
POST	/bodyasjson					controllers.Application.bodyAsJson

GET		/typeahead					controllers.SearchController.suggestion(keyword)
POST	/search						controllers.Application.search

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
